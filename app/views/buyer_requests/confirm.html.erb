<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>見積依頼完了</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
    <style>
        body {
            background-color: #f8f9fa;
        }
        .confirmation-container {
            max-width: 600px;
            margin: 80px auto;
            background: white;
            padding: 30px;
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            text-align: center;
        }
        .btn-home {
            margin-top: 20px;
        }
        .footer {
            margin-top: 30px;
            font-size: 12px;
            color: gray;
        }
    </style>
</head>
<body>

    <div class="container">
        <div class="confirmation-container">
            <h2 class="mb-4">見積依頼完了</h2>
            <h4 class="text-primary">見積依頼番号（ID）：<%= @buyer_request.id %></h4>
            <p class="mt-3">御見積依頼有難うございました。</p>
            <p>見積回答（オファー）がありました際は、<br>登録メールアドレス宛に回答いたします。</p>
            <a href="/" class="btn btn-primary btn-home">ホームへ戻る</a>
        </div>

        <div class="text-center footer">
            ©2022 NISSAN
        </div>
    </div>

</body>
</html>
