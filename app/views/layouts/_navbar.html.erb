<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container">
   <% if user_signed_in? %>
    <%= link_to "B-TRADE", authenticated_root_path, class: "navbar-brand" %>
  <% else %>
   <%= link_to "B-TRADE", unauthenticated_root_path, class: "navbar-brand" %>
  <% end %>

    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav ms-auto">
        <% if user_signed_in? %>
          <% if current_user.admin? %>
            <li class="nav-item">
              <%= link_to "管理者ページ", admin_users_path, class: "nav-link btn btn-danger text-white" %>
            </li>
          <% else %>
            <li class="nav-item">
              <%= link_to "マイページ", user_profile_path(current_user), class: "nav-link btn btn-primary text-white" %>
            </li>
          <% end %>
          <li class="nav-item">
            <%= link_to "ログアウト", destroy_user_session_path, method: :delete, class: "btn btn-secondary" %>
          </li>
        <% else %>
          <li class="nav-item">
            <%= link_to "ログイン", new_user_session_path, class: "nav-link" %>
          </li>
          <li class="nav-item">
            <%= link_to "新規登録", new_user_registration_path, class: "nav-link" %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>

